name = "ai-drama-workbench"
compatibility_date = "2025-12-09"

[assets]
directory = "./dist"  # 静态资源目录

# 单页应用路由配置（修复核心）
[[routes]]
pattern = "ai-drama.jikehu999.workers.dev/*"  # 移除 https:// 前缀
navigation_fallback = { bypass_token = "random-token", status = 200, filename = "/index.html" }
